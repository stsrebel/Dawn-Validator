const config = {
  useProxy: false, // Set to true if you want to use proxies, false otherwise | Sử dụng proxy hay không (true/false)
  restartDelay: 180, // Delay in seconds before restarting the process | Thời gian gửi ping(giây)
};

module.exports = config;
